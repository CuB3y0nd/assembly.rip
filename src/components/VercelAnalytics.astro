---
import { onMount } from 'svelte'

onMount(() => {
  if (typeof customElements !== 'undefined' && !customElements.get('vercel-analytics')) {
    import('@vercel/analytics').then(({ inject }) => {
      inject();
    });
  }
});
---
